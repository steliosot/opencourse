# Lab 1: Python and CSV Basics

Objectives:
- Load `datasets/sales.csv` with Python's `csv` module.
- Compute total units sold.
- Compute total revenue per region.

Steps:
1. Open `assessments/task1/solution.py`.
2. Inspect `load_sales` and `total_units`.
3. Extend the file with `revenue_by_region(rows)`.
4. Validate your changes using `opencourse test test-task1`.

Stretch:
- Sort regions by descending revenue and print top 2.
