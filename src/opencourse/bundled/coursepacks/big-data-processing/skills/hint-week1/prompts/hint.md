If totals look wrong, print intermediate values.

Tips:
- Cast CSV numeric fields to `int` or `float` before arithmetic.
- Start with one row and scale up.
- Use deterministic tests to confirm each function.
